---
import Layout from '../layouts/Layout.astro';
import PostCard from '../components/PostCard.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const title = "Здраволог — блог о здоровье и питании | Карпова Ольга";
const description = "Практические советы по здоровому питанию, похудению и полезным привычкам от нутрициолога Карповой Ольги. Научный подход без диет и ограничений.";
---

<Layout title={title} description={description}>
    <div class="text-center py-12">
        <h1 class="text-4xl font-extrabold tracking-tight text-gray-900 sm:text-5xl md:text-6xl">
            Советы нутрициолога о <span class="text-[rgb(var(--accent))]">здоровье и питании</span>
        </h1>
        <p class="mt-4 max-w-2xl mx-auto text-xl text-gray-500">
            Научный подход без жёстких диет. Практические рекомендации нутрициолога Карповой Ольги.
        </p>
    </div>

	<section class="mt-12">
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
            {posts.map((post) => (
                <PostCard post={post} />
            ))}
        </div>
    </section>
</Layout>
